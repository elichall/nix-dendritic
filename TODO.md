# TODO

## Phase 1 — System-level conversions (nixos.*)

- [x] `modules/configuration.nix` → `nixos.main`: full legacy `configuration.nix` body (boot, zram, locale, users, nix-ld, session vars, network/ssh/tailscale/firewall, bluetooth, audio, fuse, portal, ly, hyprland, fonts, systemPackages, flatpak, tmpfiles, nix settings, sysctl, maintenance)
- [x] `modules/mime.nix` → `nixos.mime`: customMime package + `xdg.mime.defaultApplications` (browser id inlined from var.nix)
- [x] `modules/services/tlp.nix` → `nixos.tlp`: tlp settings + power-profiles-daemon disable
- [x] `modules/programs/nvim.nix` → `nixos.nvim` + `homeManager.nvim` (system package + EDITOR/VISUAL/SUDO_EDITOR)
- [x] `modules/programs/opencode.nix` → `nixos.opencode` (system package)
- [x] `modules/hosts/workstation.nix`: import new nixos/homeManager modules; `home-manager.useGlobalPkgs`/`useUserPackages`; switch `users.<name>.modules` → `imports`
- [x] Bootstrap: `git add` flake source; `flake.nix` mkFlake list-wrapper fix; valid stubs for empty placeholder files; HM shell-integration defaults fix in `cmdLine.nix` (`home.shell.* = false`)
- [x] Groundwork for Phase 2 — extracted redundant system options out of `nixos.main` into feature modules:
  - Group 1 (dedup): removed `programs.bash.enable` (→ `nixos.cmdLine`), `neovim`/`opencode` systemPackages (→ `nixos.nvim`/`nixos.opencode`)
  - Group 2 (feature groundwork): `nixos.hyprland` (compositor + hypridle); `nixos.display` (ly, xdg.portal, WLR/OZONE session vars); `nixos.awww`; `nixos.waypaper`; `nixos.rclone` (rclone + `programs.fuse`); all wired into `workstation.nix`
  - Left in main: tailscale pkg (stretch network split), networking/hardware/audio/security bits

## Validation status (Phase 1)

- [x] `nix-instantiate --parse` — all modules clean
- [x] `nix eval .#modules` — registry: nixos = main, cmdLine, mime, tlp, nvim, opencode, hyprland, display, awww, waypaper, rclone; homeManager = main, cmdLine, nvim
- [x] Spot-check evals — all Phase 1 nixos + homeManager options resolve correctly; post-groundwork eval confirms moved pkgs present, no dedup
- [ ] Skeleton hosts (`wsl`/`ubuntu`/`macos`) — BLOCKED: use `specialArgs`; HM 26.05 requires `extraSpecialArgs` (see stretch below)

## Phase 2 — User-level conversions (homeManager.*)

- [x] `modules/home.nix` → `homeManager.main`: stateVersion/username/homeDirectory; XCOMPOSECACHE session var, pointerCursor, `xdg.enable`, base `home.packages` (portal-gtk, libqalculate, chafa, showoff deps)
- [x] `modules/programs/git.nix` → `homeManager.git`
- [x] `modules/programs/tmux.nix` → `homeManager.tmux` (plugins + extraConfig)
- [x] `modules/programs/yazi.nix` → `homeManager.yazi` (+ keymap.toml, init.lua, FILEMANAGER/TERM_FILE_CHOOSER vars)
- [x] `modules/display/ghostty.nix` → `homeManager.ghostty` (new module: package + static `xdg.configFile."ghostty/config"`; theme baseline = journal default, theme switching lands with Phase 3)
- [x] `modules/services/awww.nix` → `homeManager.awww` (awww-daemon unit)
- [x] `modules/services/waypaper.nix` → `homeManager.waypaper` (waypaper-restore unit)
- [x] `modules/services/rclone.nix` → `homeManager.rclone` (rclone-box unit)
- [x] Wire all Phase 2 homeManager modules into `workstation.nix`
- [x] Icons: copied `nixos/assets/icons/*` → tracked `modules/_assets/icons` (required for pure eval); `.gitignore` `_assets/` → root-anchored `/_assets/`; un-staged `nixos` symlink + added bare `nixos` to ignore
- [x] cmdLine reconciliation to legacy: `gdu` alias restored, starship `nix_shell.symbol` spacing, `fzf.enableBashIntegration = false`

## Phase 2.5 — Redundancy refactor (isolation for scaling)

- [x] `modules/display/tui.nix` → `homeManager.tui`: TUI launcher isolated (wlctl build, tuiApps list, `mkTuiOpen` wrappers, icon discovery/packages, all 6 desktop entries incl. yazi, and the btop/gdu/bluetui/jolt-tui/wlctl/yazi-open binaries)
- [x] Removed TUI binaries (`btop`, `gdu`) from `nixos.main` `systemPackages` — they're display-launcher targets, not core system programs; now user-scale in `homeManager.tui`
- [x] `modules/programs/nvim.nix` → absorbs the 5 global LSPs (nil, marksman, lua-language-server, texlab, bash-language-server) into `homeManager.nvim`
- [x] `modules/programs/zotero.nix` → `homeManager.zotero` (new module: flatpak `.desktop` dataFile, moved out of main)
- [x] `modules/programs/otter-launcher/` → relocated to `modules/display/otter-launcher/` (display-launcher dependency, not core program)
- [x] Ghostty/theme interface contract documented in `ghostty.nix` (sole owner of `xdg.configFile."ghostty/config"`; Phase 3 theme module must not declare it) + `mime.nix` yazi.desktop contract
- [x] `homeManager.main` slimmed to true user-globals (stateVersion, user, XCOMPOSECACHE, pointerCursor, portal-gtk, showoff deps pending Phase 3)

## Phase 2.5 — Package placement refactor (user-scale vs system-scale)

- [x] `modules/display/showoff.nix` → `homeManager.showoff`: showoff deps moved out of `homeManager.main` (tty-clock, gping, cava, cmatrix, cbonsai, asciiquarium-transparent, sl, lolcat, cowsay, weathr); wired into `workstation.nix`
- [x] `homeManager.main` = portal-gtk (MUST stay user-scale — user-confirmed, breaks system otherwise) + otter/showoff user tools (libqalculate, chafa, fastfetch, pending Phase 3 module absorption)
- [x] `nixos.main` systemPackages reduced to base utils only: unzip, wl-clipboard, xdg-utils, docker (docker service split = stretch)
- [x] `nixos.hyprland` += grimblast, brightnessctl (keybind consumers); `homeManager.yazi` += ripdrag (drag-drop dep); `homeManager.nvim` += tree-sitter; `homeManager.cmdLine` += ripgrep, tree
- [x] Full per-package placement analysis documented (all 13 nixos.main packages traced to consumers in legacy + new tree)
- [x] Directory reorg: `cmdLine.nix` → `modules/programs/` (shell program); new `modules/system/` for system-scale base config — holds `mime.nix`, future home for leaned-out `configuration.nix` (`system/main.nix`) + network/hardware/audio/security splits. Registry keys unchanged (`nixos.cmdLine`/`homeManager.cmdLine`/`nixos.mime`)

## Phase 2.5 — `services/` directory elimination (aspect relocation)

- [x] `modules/services/` removed entirely — aspect-oriented grouping (AGENTS.md Rule 1):
  - `tlp.nix` → `modules/system/battery.nix` as `nixos.battery` (was `nixos.tlp`; renamed key + host wiring updated). Fixed pre-existing battery.nix bug: it defined `nixos.audio` (copy-paste from audio template) so the key silently never registered
  - `awww.nix` → `modules/display/awww.nix`, `waypaper.nix` → `modules/display/waypaper.nix` (display aspect: wallpaper daemon + restore; keys unchanged)
  - `rclone.nix` → `modules/programs/rclone.nix` (program aspect; keys unchanged)
- [x] Header comments updated (awww/waypaper/rclone/configuration.nix dir refs); validated: registry (`nixos.battery` in, `nixos.tlp` out, `nixos.audio` single-sourced), toplevel + activation drvPaths unchanged

## Phase 2.5 — `configuration.nix` lean into `system/` splits

- [x] `nixos.network` ← `modules/system/network.nix`: hostName, networkmanager, openssh, tailscale + TS_DEBUG_FIREWALL_MODE, nftables/firewall (trustedInterfaces, tailscale port), wait-online disables
- [x] `nixos.hardware` ← `modules/system/hardware.nix`: bluetooth (powerOnBoot, Source/Sink/Media/Socket) + maintenance (fstrim, fwupd, intel microcode `= true`, earlyoom)
- [x] `nixos.audio` ← `modules/system/audio.nix`: rtkit + pipewire (alsa, alsa.support32Bit, pulse)
- [x] `nixos.security` ← `modules/system/security.nix`: boot.kernel.sysctl hardening ONLY — `nix.settings`/`nix.gc` intentionally stay in `nixos.main` (user choice)
- [x] `nixos.main` reduced to base identity: stateVersion, bootloader + zram, time/locale, users, nix-ld, fonts, base systemPackages, flatpak, tmpfiles, nix.settings/gc; header comment lists all 13 extracted feature modules
- [x] docker REMOVED from systemPackages (user: unused); all four system/ splits wired into `workstation.nix`; validated — parse OK, registry complete (21 nixos keys), toplevel drvPath `34p57aphpnv6v1zzw76fc6kjga0g7lwi` (changed by docker removal), activation unchanged, 13-option spot-check all green

## Phase 2.5 — Project-state sweep execution (system validation + `_lib`)

- [x] fastfetch made HOME-ONLY (user directive): `modules/programs/fastfetch.nix` now defines only `homeManager.fastfetch` with `home.packages = [ pkgs.fastfetch ]` (was dual nixos+home). Removed `self.modules.nixos.fastfetch` from `workstation.nix`; verified fastfetch-2.63.1 present in home.packages and ABSENT from systemPackages. AGENTS.md Rule 4 (Per-Module Dependency Self-Containment) added as core rule — every module bundles its own deps, presence vs configuration decoupled, duplicate package declarations across modules allowed/encouraged (list merge), waybar may re-declare fastfetch Phase 3. Toplevel drv `baha7iajhkl5ahqp8fkp2qshw8dhr4ik`, activation drv `jrcinqnba0f6y56yc8nly1lwbk5ccizl` (both changed intentionally by scope move)
- [x] t480 hardware wiring: `/etc/nixos/hardware-configuration.nix` → `modules/system/hardware-t480.nix` as registry module `nixos.hardwareConfig` (import-tree REQUIREMENT: bare NixOS modules must not live in `modules/` — they recurse the flake-parts eval; must be wrapped in `flake.modules.*`); added to `workstation.nix`; full `toplevel.drvPath` now evaluates
- [x] `modules/system/` split templates created (empty, NOT wired): `network.nix` (nixos.network), `hardware.nix` (nixos.hardware), `audio.nix` (nixos.audio), `security.nix` (nixos.security) — each documents its planned content from `nixos.main`; `configuration.nix` intentionally NOT moved (user decision)
- [x] `modules/_lib/browser.nix` DRY source created (appId/command/desktop for zen browser); `nixos.mime` now consumes `browser.desktop` via `import ../_lib/browser.nix`; kills the inlined-var stretch
- [x] Legacy snapshot: `/etc/nixos/{configuration,home,hardware-configuration}.nix` + `modules/**` copied to tracked `legacy/` (assets/ skipped — fully mirrored in `modules/_assets/`); frozen reference for Phase 3 ports

## Phase 2.5 — Flake input packaging (wlctl)

- [x] `wlctl` provisioned as pinned flake input `github:aashish-thapa/wlctl/v0.1.9` (`inputs.nixpkgs.follows = "nixpkgs"`); `tui.nix` now uses `inputs.wlctl.packages.${pkgs.system}.default` — tarball `fetchTarball` + local `buildRustPackage` removed
- [x] New skill `.agents/skills/package-provisioning/SKILL.md`: order of package operations (1. nixpkgs → 2. upstream flake input → 3. build from source)
- [x] otter-launcher flake input (`github:kuokuo123/otter-launcher/v0.7.6` + follows) — DONE in phase 2 (2026-08-11); port uses `inputs.otter-launcher.packages.${pkgs.system}.otter-launcher`, keeping custom wrappers (upstream HM module does not provide toggle-close/persistent-instance/dismiss-on-pointer/dual-config)
- [x] `ned` otter module hotfix (2026-08-12): fixed `\$HOME` invalid-TOML-escape (otter parse error) + `$FILE` → `$FILE_RELATIVE` guard bug; `-not -name "legacy"` → `-not -path "legacy/*"` to actually exclude the gitignored legacy tree
- [x] fastfetch customization plan DONE (2026-08-12): `homeManager.fastfetch` now enables `programs.fastfetch` with chafa block-image logo (`modules/_assets/nixos-image.png`, `type = "chafa"`, `symbols = "block"`, width 40); fastfetch dlopens libchafa at runtime but ships it in its own closure; redundant `home.packages` fastfetch entry dropped (enable covers it)
- [x] yazi icon plan DONE (2026-08-12): `homeManager.yazi` adds `theme.icon.prepend_rules` — plan dirs (Projects/ , Box/ 󰍲, .nix/ 󱄅, wallpapers/ 󰸉, .var/ , vault/ ) + config-folder package icons (hyprland/  + hyprland.{conf,lua}, ghostty/ 󰊠, otter-launcher/ , nvim/ , tmux/ , yazi/ 󰇥)
- [x] yazi icon schema BUGFIX (2026-08-12): yazi 26.x silently removed `theme.icon.prepend_rules` — 26.5.6 `Icon` struct uses `prepend_dirs`/`prepend_files`/`prepend_exts`/`prepend_globs`/`prepend_conds` (+ append_*), exact-name HashMap matching (`file.name()`, no trailing slash). Migrated rules to `prepend_dirs` (12 dirs, slash-free) + `prepend_files` (hyprland.conf/.lua); serde was silently dropping the old key (default icons shown). `[open].prepend_rules` unchanged (still valid). Verified against v26.5.6 source + official docs
- [x] fastfetch logo height BUGFIX (2026-08-12): `logo.height = 0` errored "JsonConfig Error: Logo height must be a positive integer" (schema: `null` auto or integer ≥ 1, min 1). Removed the line → auto height (aspect preserved). Reproduced error with fastfetch 2.63.1 + confirmed both omitted and -1 render correctly
- [x] fastfetch chafa `semi` BUGFIX (2026-08-12): "Unrecognized symbol tag 'semi'." on stderr — `semi` is NOT a valid chafa symbol tag (chafa 1.18.2 `parse_symbol_tag` table lacks it). fastfetch passes `chafa.symbols` raw to `chafa_symbol_map_apply_selectors`, which fails; fastfetch prints the error and continues + caches (`~/.cache/fastfetch/images/<img>/<WxH>/chafa`, keyed image+width only) → error only on cold cache (couldn't repro pre-fix due to cache hits). Fixed: `symbols = "block"`, dropped invalid `canvas = "full"` key (schema: fgOnly/symbols/canvasMode/colorSpace/ditherMode), corrected "statically linked" header claim (runtime dlopen, self-contained closure, no chafa home.packages entry per user decision). Verified `chafa --symbols block` clean + recache re-render planned
- [x] fastfetch empty-modules BUGFIX (2026-08-12): after deploy, `fastfetch` printed ONLY the block logo — no OS/Host/etc. Root cause (fastfetch 2.63.1 source, `src/common/impl/jsonconfig.c` `printJsonConfig`): when a config file is loaded, fastfetch prints ONLY the modules listed in the config's `modules` key; a config lacking `modules` prints nothing but the logo (NO default-structure fallback — `useJsonConfig` is set on `structure.length == 0 && configDoc` regardless of the key). Reproduced with empty `{}` config + confirmed `--structure os:host:kernel` works; our deployed config was logo-only. FIX: added explicit `modules` list = full default structure (`fastfetch --print-structure`: title separator os host kernel uptime packages shell display de wm wmtheme theme icons font cursor terminal termfont cpu gpu memory swap disk localip battery poweradapter locale break colors) to `settings` in fastfetch.nix. Verified: parse OK, toplevel `jg24kbz10xiv103yk4y4vykchkfy8m7p-...` builds, generated config (`/nix/store/z5dk47cssxmxv0i3nhwg3db8jzjcxx7b-home-manager-files/.config/fastfetch/config.jsonc`) has `modules`, runtime `fastfetch --config <new> --logo-recache` = exit 0, empty stderr, logo + full info modules
- [x] opencode power-user plan DONE (2026-08-12, see `modules/_assets/plans/opencode-implementation.md`): `homeManager.opencode` expands — `poppler-utils` + Nix-managed GLOBAL `/pdf` command (`xdg.configFile."opencode/commands/pdf.md"`, available in every directory/session; template instructs agent: pdftotext -layout → temp → read tool, scanned-PDF `pdftoppm` escalation, `pdfinfo` metadata). `ocread` bash wrapper REMOVED (superseded — nested headless `opencode run` vs keeping current session). Nix-owned `xdg.configFile."opencode/tui.json"` (theme system + `scroll_acceleration` boolean fix — live `"true"` string made the whole file silently skipped by the exact-schema TUI). §2 custom commands dropped (user: example only). §3 vim mode NOT implemented — verified NO released opencode ships `keybindings = "vim"` (v1.15.10 source: config.ts/tui-schema.ts/keybind.ts/CHANGELOG.md + live schema + repo tree search); install-opencode.com changelog claim inaccurate; no pinning needed. Plugin deferred → stretch
- [/] `vault/` obsidian glyph U+E6BB: `nf custom obsidian` added in nerd-fonts 3.5.0 (2026-08-02, PR #2031); nixos-26.05 pin still 3.4.0 → renders "?" until stable bump (deferred per user; flake.lock bumped 2026-08-12, nixpkgs fcb8fcd→70cc455)
- [x] otter `tsm` tmux session manager DONE (2026-08-13): new `[[modules]]` block (prefix `tsm`, after `ssh`, before `th`) mirroring tmux-fzf `<prefix> s` (switch/new/rename/detach/kill/[cancel]) + one-line form `tsm <action> <session>`. Argument handling verified from otter 0.7.6 source (`main.rs:162-177`): otter passes the full prompt-minus-prefix (spaces preserved) via `{}` interpolation ONLY (no env) — cmd captures once (`A="{}"`) then `set -f; set -- $A` splits into action/session query. fzf `-1` (man-verified "do not start interactive finder… automatically select the only match") makes `tsm sw main` fully non-interactive (action auto-selects, session auto-selects, attach fires); partial/empty args degrade to interactive finder. Dispatch: switch → `setsid -f ghostty -e tmux new-session -A -s`; new → name from arg or `read` prompt; rename → `read` + `rename-session -t`; detach/kill → `--multi` + `while read` loops (kill via `sort -r`), current session NOT excluded (user: killing the launching session is fine), empty target → no-op exit (graceful, same as switch). Windows live-preview via fzf `{1}` (no bare `{}` anywhere except the single `A="{}"` capture). `otter.nix` diagnose comment `tmux (pro/ssh/tsm modules)`; contract row in module-contracts.md updated. Validated per app-config-data scoping (NO full toplevel build): targeted configFile-source build OK (store `/nix/store/ikfxlnx79mwnl5qri19639r73qmkwv8n-otter-launcher.toml`), `builtins.fromTOML` = 13 modules, zero residual `@TOKEN@`, only `{1}` placeholders, `sh -n` OK, scratch pty tests confirm `set -- $A` split + fzf `-1`/`--multi -1` auto-exit + no-match→finder graceful path
- [x] otter FIRST-PRESS latency FIX (2026-08-15): first `otter-open`/`otter-power` press after boot was slow — the headless ghostty single-instance server that `otter-launch` hand-offs to via `+new-window --class=com.otter.launcher` was autostarted LAST in the `hyprland.start` block, so an early press cold-started ghostty (2-5s). Measured warm: `+new-window` hand-off 0.09s, first-press launcher ready 0.24s, chafa overlay 0.035s — all fast; the only cold path is the boot race. Fix: extracted the server command to let-bound `otterServer` in `modules/display/hyprland.nix` (`ghostty --class=com.otter.launcher --initial-window=false --quit-after-last-window-closed=false --gtk-single-instance=true`) and moved its `hl.exec_cmd("${otterServer}")` to the TOP of the autostart block (before tmux/waybar) so it is up at the earliest moment of login, headless (invisible). Validated via dry eval of `wayland.windowManager.hyprland.extraConfig` (server exec first, store-path ghostty). User: rebuild + deploy + next-boot first-press should be instant
- [x] nvim DICTIONARY EXPANSION DONE (2026-08-16, plan `modules/_assets/plans/dictionary-expansion.md`): scientific/physics/engineering vocabulary for md/tex via (A) ltex-ls primary (grammar+spell LSP, nixpkgs v16.0.0, `home.packages`) and (B) vim builtin spell backbone (compiled wordfile). Build-time inflection expansion: pure-Nix `expandWordlist` generates possessive `'s` + regular plurals (skip: `-ly`, `-s`, digit-final, pluralSkip); three-file output — `en.utf-8.add` (base, readable), `en.utf-8.expanded` (base+inflections), `en.utf-8.add.spl` (mkspell of expanded). Case enforcement: acronyms ALL-CAPS (`AFSD`, `PDE`, `PINN`, `GPU`, …), proper model names Title-Case (`Johnson-Cook`, `Zener-Hollomon`, …) — vault-verified; lowercase acronyms now rejected (professional policy, user-approved). Digit handling: parser regex includes `0-9`; `pluralOf` skips digit-final (`AA70500` prevented). `tools/harvest-spellbad` is stdout-only (additive); reads `en.utf-8.expanded` when present; known-set stored lowercase (vocabulary-focused, no case-violation noise). ltex-ls `load_scientific_dictionary()` prefers `.expanded`, falls back `.add`. Validated: derivation builds (`/nix/store/bj3yg88…`), 621-line expanded correct, isolated spell matrix ALL PASS (caps names + lowercase common words pass; lowercase acronyms/names + garbage rejected). User: re-run `snorbs` (force=true fix staged but never deployed), test ltex-ls over-acceptance
- [x] otter `tsm` boot visibility FIX (2026-08-14): `tmux list-sessions` needs a live tmux server, so after a reboot `tsm` saw no sessions until tmux was opened once. Added hyprland.autostart boot: `hl.exec_cmd("bash -c '${pkgs.tmux}/bin/tmux has-session -t main 2>/dev/null || ${pkgs.tmux}/bin/tmux new-session -d -s main'")` — headless server start triggers continuum `@continuum-restore 'on'` (modules/programs/tmux.nix) to restore saved sessions, `main` guard keeps the server alive (mirrors the C-X / M-C-X keybind convention). Placed first in the `hyprland.start` block. Validated via dry eval of `wayland.windowManager.hyprland.extraConfig` (command present, no eval errors). User: rebuild + deploy + restart hyprland; tsm should list sessions pre-manual-tmux
- [x] TLP BAT1 charge-threshold FIX (2026-08-14): module only set `START/STOP_CHARGE_THRESH_BAT0` — the T480's second (removable) battery BAT1 had no thresholds (sysfs 0/100) and charged to 100% on AC, appearing to ignore the charge-level config. Added `START_CHARGE_THRESH_BAT1 = 75` / `STOP_CHARGE_THRESH_BAT1 = 80` to `modules/system/battery.nix`. Verified live: BAT0 sysfs start/end = 75/80 (applied), TLP boot log clean ("Setting battery charge thresholds...done."). BAT1 threshold application to verify post-deploy (`sudo systemctl restart tlp` or next boot). `PLATFORM_PROFILE_ON_AC/BAT` KEPT (no-op on T480 — hardware exposes no `platform_profile`; user moving to Framework 13 Pro which supports it). Contract row + legacy row unchanged otherwise
- [x] waybar hover squares + battery full-at DONE (2026-08-14): (1) hover-feedback CSS rule added to `modules/display/waybar.nix` style block — `#clock:hover, #custom-weather:hover, #cpu:hover, #temperature:hover, #memory:hover, #custom-system:hover, #bluetooth:hover, #network:hover, #battery:hover, #workspaces button:hover { background-color: rgba(0,0,0,0.2); }` — instant (no transition), value byte-identical to waybar's inherited default `style.css` `#workspaces button:hover`, `#custom-nixos` EXCLUDED (owns its `#89dceb` hover). (2) battery `full-at = 80` added — verified in waybar 0.15.0 source (`battery.cpp`): `calculated_capacity = 100 * capacity / full_at` scales icon + `{capacity}` number + warning/critical states (30/15 now trigger at 24%/12% actual) so the full icon `󰁹` appears at the 80% TLP cap. User chose scaled display (80% cap = "100%"). Reload = SIGUSR2 (default action = reload per `main.cpp` `handleUserSignal`), matches theme.nix `pkill -USR2 waybar` convention. Validated per app-config-data scoping: `programs.waybar.settings.mainBar.battery` eval shows `full-at: 80`, `programs.waybar.style` eval contains the hover rule. Contract row updated. REVISED (same day): user wanted the hover to NOT alter the pill's transparent boundary — original rule overrode `background-color` 0.5→0.2 (whole pill washes out). Rewritten as boundary-preserving INNER BLOCK: non-workspaces modules get `background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2))` at `background-size: 60% 60%` centered no-repeat (composites to ~0.6 black block inside; edge stays exactly 0.5); `#workspaces button:hover` split back to its own full-fill rule (transparent button, no pill boundary). GTK3 supports `background-size`/`background-position` (verified vs docs.gtk.org/gtk3 CSS reference); no pseudo-elements/radial-gradients in GTK3. Re-validated: `programs.waybar.style` eval contains both rules. Contract row updated to "boundary-preserving inner-block hover". REVISED #2 (same day): user wanted the hover box to have an OUTLINE + ROUNDED EDGES. GTK3 constraint: `border`/`inset box-shadow`/`border-radius` apply only to the element box (the whole pill), and background-image layers can't be rounded (no radial-gradients/pseudo-elements in GTK3). Solution: style the module's inner `label` child (waybar modules = box→label) — transparent 2px border + `border-radius: 8px` always present (layout stable), `border-color: rgba(0,0,0,0.35)` + `background-color: rgba(0,0,0,0.2)` on `#<module> label:hover` → rounded outlined chip hugging the content, inside the pill, pill boundary untouched. `:hover` fires only over the label content area (not the pill padding ring). `#workspaces button:hover` kept as separate full-fill rule. Re-validated: `programs.waybar.style` eval contains the label-chip rules. Contract row updated to "rounded outlined label-chip hover". REVISED #3 (same day): label-chip approach DEAD — GTK3 pointer tracking requires an input window; GtkLabel/GtkBox have none (GTK maintainer ebassi quote), so `#<module> label:hover` never fires → zero hover effect. Waybar's canonical hover target is the module BOX (`#pulseaudio:hover { box-shadow: inset 0 -3px #ffffff; }` in the default style.css); `outline` is GTK's focus-rectangle mechanism, unused in waybar's default css. FIX: negative-spread inset box-shadows on the module boxes — `box-shadow: inset 0 0 0 -5px rgba(0,0,0,0.2), inset 0 0 0 -3px rgba(0,0,0,0.35)` (two layers = crisp 2px darker outline + filled inner block, both rounded per the pill radius, boundary `rgba(0,0,0,0.5)` untouched; blur 0 = hard block). Always-transparent default not needed: non-hover modules have no shadow rule → default `none`. Module boxes fire `:hover` (waybar's documented pattern). Re-validated: `programs.waybar.style` eval contains the box-shadow rules. Contract row updated to "inset box-shadow rounded inner-block hover". REVISED #4 (same day): negative-spread inset box-shadow rendered NOTHING on hover — GTK3 drops the declaration (negative spread not supported → no effect at all). Root cause: only the module box has an input window (hover-capable) but its background/border can only style the whole pill; the label can render rounded/outlined blocks but is not hover-capable. FIX: DESCENDANT SELECTOR — hover fires on the module box, styles its inner label: always-on transparent `border: 2px` + `border-radius: 8px` on `#<module> label` (layout stable), then `#<module>:hover label { border-color: rgba(0,0,0,0.35); background-color: rgba(0,0,0,0.2); }` → rounded outlined chip inside the pill, boundary `rgba(0,0,0,0.5)` untouched, full pill is the hover target (not just the label area). Workspaces keeps its full-fill rule. Re-validated: `programs.waybar.style` eval contains both label rules + hover rules. REVISED #5 (same day): descendant-label chip STILL nothing — GTK FAQ settles it: "GtkLabel... draw themselves directly onto their parent's window... to set the background color for a GtkLabel, change the background color of its parent" → labels paint ONLY text color, never background/border. User chose SQUARE INNER BLOCK (kept the workspaces rounded-box hover as reference — those numbers are GtkButtons, windowed widgets that DO render rounded hover boxes; pill modules' text is labels, no per-number box possible). FIX: hover on the module box draws `background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2))` at `background-size: 60% 60%` centered no-repeat — dark block inside the pill, boundary `rgba(0,0,0,0.5)` untouched, square corners (GTK3 can't round it: no radial-gradient/pseudo-elements). Box renders backgrounds (proven: pills); this is the ONE boundary-preserving block that reliably renders. Rounded whole-pill darken (`0.5→0.65`, stays opaque) remains as the workspaces-style alternative. Re-validated: `programs.waybar.style` eval contains the inner-block rule. REVISED #6 (same day): investigated "make modules buttons via custom/*" — source confirms waybar text modules are NOT boxes but windowed `Gtk::EventBox`es (`AModule::operator Gtk::Widget&()` returns `event_box_`; `ALabel.cpp`: `event_box_.add(label_)`), which is why `#module:hover` fires and why pills render rounded. `custom/*` modules are `Custom : ALabel` — same EventBox+label tree, NO button gained. Only GtkButtons (workspaces numbers) paint their own rounded hover boxes; the module's ONLY paint surface is the EventBox (= whole pill; the label child paints nothing). User chose WORKSAPCES-STYLE ROUNDED PILL DARKEN: `#clock:hover,... { background-color: rgba(0,0,0,0.65) }` — whole pill darkens, stays opaque (boundary not washed out), rounded per pill radius, matches the workspace-button hover aesthetic. Bonus documented: `AModule::handleMouseEnter` sets `:prelight` on the label, so `#module label:hover { color }` (text brighten) also renders. Re-validated: `programs.waybar.style` eval contains the 0.65 darken rule

- [x] `init-project` scaffold DONE (2026-08-14, user-added module `programs/utils/initProject.nix` + `groups/utils.nix` + workstation wiring; agent hardended): rewritten to `pkgs.writeShellApplication` (shellcheck at build). Fixed latent bugs: indented closing `EOF` meant the `.gitignore` heredoc NEVER terminated (swallowed `git add`/`direnv allow`/`git commit` at runtime) — all heredocs now flush-left; `set -euo pipefail`; bails with exit 1 if `./.git` already exists; `git init -b main`; removed redundant `touch .gitignore`; typo "Recourses"→"Assets"; dropped `.python_version` (wrong pyenv name; `.python-version` committed under uv). Build-env scaffold: `uv init` (pyproject.toml/main.py/.python-version) + minimal `flake.nix` devShell template (nixfmt/uv/gcc/clang-tools/cmake) using `system = "x86_64-linux"` — first draft used `builtins.currentSystem` which is IMPURE and failed pure `nix eval`; fixed to literal system + `devShells.${system}.default`. Rule 4 deps git/direnv/uv in home.packages. Validated: toplevel build green (both `init-project.drv` + previously-failing `home-manager-path.drv`), scratch-dir run → git repo on `main`, 6 files committed, `use flake` envrc, `.envrc`/`AGENTS.md`/`.agents/` ignored, generated flake evaluates pure (`devShells.x86_64-linux.default.name` = nix-shell), guard refuses in existing repo. Gotcha logged: `ls -dt /nix/store/*-init-project` ties on identical store mtimes — identify builds by content markers, not glob ordering (accidental run of a stale user build overwrote repo `.gitignore`; restored via `git checkout`). Contract rows for `initProject` + `utils` group + host wiring updated
- [x] otter `th` theme preview DONE (2026-08-13, plan `otter-theme-module-wallpaper-display.md`): `th` module now previews each theme's wallpaper (chafa kitty-protocol image sized to the pane) + 16-color ANSI swatch. `swatchScript` (`theme-swatches`) added to `homeManager.theme`, runs from `initTheme` after sync every activation — loops `profiles/*.json`, resolves each `ghostty_theme` palette via headless ghostty (`XDG_CONFIG_HOME=$(mktemp -d) +show-config`, parse `palette = N=#HEX`), writes `generated/previews/<name>.swatch` (16 ANSI bg blocks only), guarded per profile. `config.toml` `th` block: `--preview` (jq `.wallpaper` → chafa, then cat swatch) + `--preview-window=right:50%,wrap`; new tokens `@THEME_DIR@`/`@THEME_SWATCHES@` in `mkOtterConfig`; `jq` added to `homeManager.otterLauncher` home.packages + `otter-diagnose` check. Contract C19 in module-contracts.md (theme owns profiles+swatches, otter consumes). Validated: parse OK, toplevel + activation + configFile-source builds green, generated config.toml TOML-valid with no residual tokens, direct build confirmed preview block present (earlier store inspection was a stale home-manager-files), 12/12 swatches generated matching real palettes (journal = Melange Dark verified), wallpaper+swatch preview pipeline renders in pty. POST-DEPLOY BUGFIX (2026-08-13, two iterations): (1) preview showed "no palette swatch" for every theme — REAL root cause (from otter-launcher 0.7.6 source, `mod_exec.rs`): otter replaces EVERY bare `{}` in a module cmd with the module argument (empty for `th`) before `sh -c`, so `profiles/{}.json` → `profiles/.json` → `-f` checks failed. Earlier `-q '{}'`→`-q ''` and unquoting were red herrings (otter already replaced `-q '{}'`→`-q ''`). Fix: use fzf `{1}` in both preview paths (otter's exact-`{}` replace never matches; single-token names make `{1}` ≡ `{}`). Verified in tmux pty. (2) chafa `-s 18x18` without `-f symbols` → kitty-graphics real images → fzf's text clearing can't erase them → stale/stacked wallpapers, and the fixed size overflowed the ~26-34x12-15 preview pane (550x250 launcher) covering the swatch. Fix: preview emits kitty delete-all first (`printf "\033_Ga=d,d=A\033\\"`, TOML-escaped `\\033`/`\\\\`), sizes chafa via `-s "${FZF_PREVIEW_COLUMNS:-34}x$((FZF_PREVIEW_LINES-3))"`, swatch trimmed to blocks-only. Second fix (same day): swatch still hidden under the image — fzf lays out preview text independently of the kitty image (chafa `\e_G` = one invisible text line → swatch text landed at pane top under the image box); preview now emits `$LN` blank lines after chafa to push the swatch below the image (rows 0..LN-1 → swatch at LN+1). REQUIRED post-switch (user): `sudo nixos-rebuild switch --flake ~/.nix#workstation`, then `th` shows one wallpaper image with palette blocks BELOW it, no stacking

## Phase 3 — Legacy nixos/modules/ ports (deferred, homeManager.*)

### Bulk pass (non-coupled modules moved first — COMPLETE)

- [x] `_assets/wallpapers` → NEW `homeManager.wallpapers` (`modules/display/wallpapers.nix`): provisions all 17 tracked wallpapers into `~/Pictures/Wallpapers` via `home.file` symlinks (12 theme-profile + 5 waypaper library); wired into `homeManager.desktop` group
- [x] `legacy/modules/showoff.nix` → `homeManager.showoff` scripts/configs: showoff dispatcher + showoff-layout tmux grid + term-rotator `writeShellApplication` scripts; `hypr/hypridle.conf` (300s idle listener) + `tmux/showoff.conf`; ghostty declared per Rule 4 (showoff TERMINAL)
- [x] `legacy/modules/desktop-stable.nix` → `homeManager.waybar`: `programs.waybar` settings/style, `systemd.enable = false` (UWSM tracks execution); user-scope binaries absolutized (ghostty, fastfetch, weathr, wlctl, bluetui, jolt, btop), system-scope (nmcli/bluetoothctl/grep) PATH-based, otter-open deferred; Rule 4 deps declared
- [x] `legacy/modules/hyprland.nix` → `homeManager.hyprland` full lua config: `wayland.windowManager.hyprland` (configType lua, systemd.enable=false), browser var inlined from `_lib/browser.nix`, monitors/look-feel/keybinds/autostart/submap/workspace+window rules; user-scope binaries absolutized (ghostty, tmux, yazi, waybar, way-edges, waypaper, hypridle, grimblast, brightnessctl, playerctl), system-scope (wpctl/hyprctl/systemctl) PATH-based, otter/showoff wrappers deferred
- [x] FIX: command-path let-bindings shadowed `pkgs.*` attrs → home.packages got path strings (toplevel type error); renamed to `*Bin`/`*Cmd`
- [x] Validated: parse OK; registry nixos 15 keys / homeManager 21 keys (wallpapers, waybar added); toplevel drv `1z4nnbvclb6rjsxz3fpj5qdyzaxsna3l-nixos-system-t480-nixos-...drv` + activation drv `hrylwjw1ii701r52jl5cw6mi6dwfmplv-home-manager-generation.drv` both evaluate; spot-checks: 17 wallpaper home.file entries, 3 showoff scripts + hypridle/showoff configs, waybar command strings (absolutized + otter-open PATH), hyprland extraConfig interpolations (store paths present), all 10 hyprland deps are real packages

### Focused passes (one at a time — otter and theme, too coupled for bulk)

- [x] `modules/display/theme.nix` → `homeManager.theme` (theme profiles/sync/switch; drives ghostty theme at runtime per ghostty.nix interface contract — does NOT declare `xdg.configFile."ghostty/config"`; runtime rewrite only). Ported from LIVE /etc/nixos/modules/theme.nix (NOT legacy/ — legacy carries the gtk.css transparency bug; live fix documented in /etc/nixos/assets/ghostty-transparency.md). 12 profiles (beach added beyond legacy); DEFAULT_THEME = journal. Components: sync-ghostty (jq parse of `ghostty +show-config`, 9-color extraction, generated hypr/waybar/tmux/nvim/cava/gtk files + runtime symlinks + reloads), theme-switch (sed ghostty `theme =` + waypaper/swaybg wallpaper + exec sync), theme CLI (switch/list/current/reload), home.activation.initTheme (after writeBoundary: writes all profiles + default active.json), enable=false guards for hypr/waybar/tmux palette paths, cava config `theme = nixos-generated`, home.packages += jq. GTK transparency fix applied (window.background scoping). ghostty.nix updated to live values: `window-decoration = true`, `background-opacity = 0.70`. Wired into homeManager.desktop group. Validated: parse OK; registry homeManager 22 keys; toplevel drv `9h0sy8a607qfm01f97mnij92awxffzd1-...drv` + activation drv `1ivbjkxaxd5lpfcspmvxk5gpwnikfc8c-...drv` evaluate; spot-checks: theme bin + jq present, initTheme + writeBoundary present, cava config correct, no ghostty/config conflict (theme module stays the runtime-only owner)
- [x] ZERO-BASELINE REFACTOR (user architecture directive): theme persistence via path indirection — `modules/_lib/theme.nix` (shared THEME_DIR/active/generated/ghosttyThemeConf paths); ghostty.nix drops `theme = Melange Dark` baseline, adds `config-file = .../generated/ghostty/theme.conf` (stable path → HM switch can never reset theme); sync-ghostty writes theme.conf from active.json (single source of truth) BEFORE `ghostty +show-config` resolve, ghostty absolutized to store path (activation runs pre-PATH); theme-switch drops gnused sed entirely (write active.json → wallpaper → exec sync); initTheme re-runs sync after EVERY HM switch (bootstrap + consistency re-link; headless-safe, reloads guarded); user's preferred ghostty values kept (window-decoration=false, background-opacity=0.90, commit cadb532). active.json persists inherently — documented in state-implementation.md as deliberately NOT a services.state item (would be systemd overkill for a user file); `~/.local/share/theme/` = future impermanence persist path. Validated: parse OK; toplevel drv `1cf5za1nm7qdl84v7y2k0l1jdf8ngrp5-...drv` + activation drv `zh953z1zh9hs5kaprkrmz1rn64w76x14-...drv` evaluate; ghostty config contains config-file line + NO theme line + user's values
- [x] GTK PALETTE-ONLY CONTRACT (live port 2026-08-11, follow-up to the transparency fix): live theme.nix scaled the generated GTK CSS back to palette-ONLY — `~/.config/gtk-{3,4}.0/gtk.css` is user-priority CSS loaded by EVERY GTK3/4 app, so element rules leak (transparency incident: opaque `window` rule → opaque surface region → no blur/alpha; waybar incident same day: `label { color }` → uniform-white workspace numbers; `.drag` themed ripdrag pill). Ported to sync-ghostty: removed all element rules (window, window.background/.background, headerbar/.titlebar, label, selection, scrollbar, .drag) — 18 @define-color lines only; header contracts updated in theme.nix + ghostty.nix; postmortem mirrored to tracked `modules/_assets/documentation/ghostty-transparency.md` (Files table → dendritic module path). Validated: parse OK; toplevel drv `5a6jaqjafiidzjlhvm7j5m41yq7w8mym-...drv` + activation drv `4bmjcdjmchyijvg6r0y6ji27dv76jwad-...drv` evaluate; no element selectors remain in GTK block (22 @define-color = 18 gtk + 4 waybar); doc matches live
- [ ] `modules/display/otter-launcher/otter.nix` → `homeManager.otterLauncher` (flake input `github:kuokuo123/otter-launcher/v0.7.6` + follows; copy config.toml, app-launcher.sh, dismiss-on-pointer.sh into module dir; keep custom wrappers)
  - OTTER FOCUS — three-phase plan (see `modules/_assets/documentation/decisions.md` #24): 1. refine legacy → 2. port → 3. refine ported result. Phases 1-2 DONE (2026-08-11):
  - [x] **Live diagnostics**: deployed system is legacy `/etc/nixos` (Lua hyprland); confirmed ROOT CAUSE of broken dismiss — `dismiss-on-pointer.sh` greps `pgrep -f "/bin/otter-launcher"` but the real process cmdline is `otter-launcher` (bare argv0) → wait loop times out (3s), watcher bails, dismiss never fires. `hyprctl dispatch closewindow`/`movecursor` blocked by Lua `hl.dispatch` hook on this system. VERIFIED fix: `pkill -x otter-launcher` (exact comm; launcher dies, window closes, persistent server survives)
  - [x] **`modules/_lib/interaction-watch.nix`** (new, tracked): shared process-agnostic pointer watcher (model on showoff's WORKING watcher); `--tag/--grace/--interval/--bail-pattern/--on-move`; wrapper prepends `set -o errexit nounset pipefail` — guards verified; fixed shellcheck SC2034 (TAG read no-op — exists for cmdline-based `pkill -f "interaction-watch --tag NAME"` reaping)
  - [x] **showoff.nix consumes interaction-watch**: inline `showoff_watcher` block replaced; `kill_showoff` pkill → `interaction-watch --tag showoff`; added to showoff runtimeInputs + home.packages
  - [x] **`modules/_assets/documentation/decisions.md`** (otter decisions consolidated): critique (dep web, hardcoded paths incl. nrb flake-revert hazard, duplication, dead code), dependency topology map, window-class contract, interaction-watch spec, Rule 4 Hybrid exception (module-owned CLIs via otter-diagnose + activation warn), 3-phase roadmap + port checklist
  - [x] **Legacy scratch diff** (`/tmp/opencode/otter-legacy-refine/`, user-applied to /etc/nixos): nrb → `--flake "$HOME/Projects/nix-dendritic"`; ned → repo tree; `otter-launch` runtimeInputs += ghostty; `app-launcher.sh` → writeShellApplication (fzf chafa gawk grep coreutils util-linux) + drop dead `--blocks`; collapsed duplicate config tokens (2); dk/obs commented out; `otter-diagnose` binary audit added (18 checks); overlay → repo asset; stale flake comment → port note. Dismiss fix DEFERRED to phase 2 per user decision
  - [x] **Phase 2 (port)** — `homeManager.otterLauncher` DONE (2026-08-11): flake input `otter-launcher` added to flake.nix (follows nixpkgs; upstream v0.7.6 ships a flake exposing `packages.<system>.otter-launcher` — verified + built `/nix/store/d12pxdyj...-otter-launcher-git`, runs `otter-launcher 0.7.6`); `otter.nix` → homeManager-only scope (no nixos.otterLauncher — every otter concern is user-scale; window rule/keybinds/autostart live in homeManager.hyprland, waybar on-click in homeManager.waybar — documented in module header); `dismiss-on-pointer.sh` REPLACED by `_lib/interaction-watch.nix` (`--tag otter --bail-pattern '^otter-launcher([[:space:]]|$)' --on-move 'pkill -x otter-launcher'` — anchored regex verified non-self-matching, exact-comm pkill verified live in phase 1); legacy `pgrep -f "/bin/otter-launcher"` toggle fixed to `pgrep -x otter-launcher`; `otter-diagnose` (18 checks, exit 1 on missing) + `home.activation.otterDiagnose` warn hook (never fails build); config.toml + app-launcher.sh moved into module dir (import-tree ignores non-.nix); `@OVERLAY_IMAGE@` token → `$HOME/Projects/nix-dendritic/modules/_assets/nixos-image.png` (no more hardcoded /etc/nixos path); Rule 4 deps declared in home.packages (ghostty fzf chafa libqalculate wl-clipboard neovim tmux tailscale xdg-utils procps util-linux gawk gnugrep); qalc/chafa absorbed from homeManager.main; wired into `homeManager.desktop` group. Shellcheck fixes on port: `pkgs.grep` → `pkgs.gnugrep` (no such attr); app-launcher.sh SC2295 (`${base%"$ext"}`) + SC2016 (fzf owns `{}`/`$FZF_*`, disable comment). Validated: toplevel `cg97br2z1msyig1vfa878rfv00d8syv5-...drv` + activation `rcs6378bywjpg475w3phxvdb20a1g95c-home-manager-generation` eval; all otter wrapper drvs shellcheck-clean; generated config.toml verified (tokens collapsed, overlay path injected, 12 active modules, dk/obs commented)
  - [ ] Phase 3 (refine ported): waypaper/btop+ filters, nsp/gg → `_lib/browser.nix`, ssh module known_hosts, dk/obs completion, shared ghostty spawn-class helper
- [ ] `modules/display/waybar.nix` → `homeManager.waybar` (desktop-stable.nix port) — DONE, superseded by bulk pass
- [ ] `modules/display/showoff.nix` → `homeManager.showoff` scripts/configs port — DONE, superseded by bulk pass
- [ ] `modules/display/otter-launcher/otter.nix` → `homeManager.otterLauncher` — DONE, superseded (stays focused pass)
- [x] REDUNDANCY PASS (Phase 3 review, pre-otter): (1) dropped duplicate `waypaper --restore` from hyprland autostart — systemd `waypaper-restore` (Requires awww-daemon) is sole mechanism; (2) Rule 4 self-containment: tui.nix += ghostty/yazi (wrappers), showoff.nix += fastfetch/btop (tmux panes); (3) WALLPAPER MODULE MERGED INTO `homeManager.theme` — deleted `modules/display/wallpapers.nix` + `_lib/wallpapers.nix`, theme.nix now provisions all 17 wallpapers via `home.file` (12 profile + 5 waypaper library; single module → drift adjacent); removed `homeManager.wallpapers` from desktop group/host; registry homeManager 21 keys; drvPaths: toplevel `kr4h10f876wdpnhvidbwr3bghf1b446w` + activation `pnn3z69i11gas24rf70zdqa958jp88zi`; (4) transient bug in 127604e: inline `assert` over profile wallpapers forced config.home.homeDirectory during module-def eval → infinite recursion; resolved by merging wallpapers in (assert dropped, check now structural)
- [x] WAYPAPER RESTORE FIX (2026-08-11, reverts redundancy-pass (1)): wallpaper was NOT restored after rebuild — `waypaper-restore.service` + `awww-daemon.service` were both dead because `graphical-session.target` NEVER activates under UWSM (`default.target` is active; only rclone-box with `WantedBy = default.target` ran). Reverted to hyprland autostart: `homeManager.hyprland` now `hl.exec_cmd("awww-daemon")` + `hl.exec_cmd("sh -c 'sleep 0.5 && waypaper --restore'")` (absolute store paths, Rule 4 deps awww+waypaper added to home.packages); systemd units removed from `awww.nix`/`waypaper.nix` (modules reduced to binary provisioning). Smoke test §8: replaced dead unit checks with awww-daemon process + hyprland.lua restore-wiring checks (kept rclone-box). Docs updated: contracts C9/C10 (new C10 = hyprland autostart launch), decisions.md #23 (marked REVERTED), maintenance.md units note, registry rows. Validated: parse OK; registry unchanged (nixos 15, homeManager 22); toplevel drv `k1qq9sirbkxkqc785ap0nh4jzybrfsk6-...drv` + activation `q8cf5d9imdm4zy86p7q9aafvi4ifni2x-home-manager-generation.drv` evaluate. REQUIRED post-switch (user): wallpaper restores to beach.jpg on login + smoke test §8 passes
- [ ] Post-switch login smoke test REQUIRED (user, extends Phase 2.5): hyprland keybinds (super+F otter menu, super+S showoff, print-screen grimblast, media keys playerctl, XF86 brightness/volume), waybar modules (weather exec, tui on-clicks, otter-open), hypridle 300s showoff, waypaper restore

## Phase 2.5 — Aspect groups (registry presets)

- [x] `modules/groups/base.nix` (`nixos.base` = battery/network/hardware/audio/security), `desktop.nix` (`nixos.desktop` = display/hyprland/awww/waypaper/mime; `homeManager.desktop` = ghostty/tui/zotero/showoff/awww/waypaper), `toolbox.nix` (`homeManager.toolbox` = cmdLine/git/tmux/nvim/yazi) — aggregate presets via `{ self, ... }` + `imports` (flake-parts registry strictly 2-level, no nested namespaces)
- [x] `workstation.nix` collapsed to groups + singles (system: main/hardwareConfig/base/desktop/cmdLine/nvim/rclone/sandbox; home: main/toolbox/desktop/researchGroup/utils/clipboard/rclone/fastfetch); custom.terminal = "foot", noctalia shared; registry verified; all evals pass
- [x] Hash-granularity gate CLOSED (user directive 2026-08-10): drvPath/package-order differences are NOT a validation signal unless something is actually breaking — only eval success + config equivalence matter going forward

## Phase 2.5 — User-scope placement refactor (nixos → homeManager)

Package-scope study (per-module consumer tracing) + moves, all user tooling with zero system-level consumer to `home.packages`:
- [x] NEW `modules/system/clipboard.nix` → `homeManager.clipboard` (wl-clipboard; core cross-host clipboard integration, future `nixos.clipboard` aspect may grow); `wl-clipboard` removed from `nixos.main`
- [x] `awww`/`waypaper`: `nixos.awww`/`nixos.waypaper` keys REMOVED; binaries + user units now home-only (`home.packages` + absolute-path `ExecStart`)
- [x] `rclone`: binary → `homeManager.rclone`; `nixos.rclone` keeps `programs.fuse.userAllowOther = true`
- [x] `opencode`: `nixos.opencode` → `homeManager.opencode` (pure user CLI)
- [x] `hypridle`/`grimblast`/`brightnessctl`: out of `nixos.hyprland` (compositor only) into new `homeManager.hyprland` (packages only, dormant until Phase 3 config); PATH caveat documented — Phase 3 keybinds/autostart must use absolute store paths or HM user-service PATH (graphical session does not reliably carry the HM profile on PATH)
- [x] Group updates: `nixos.desktop` = display/hyprland/mime; `homeManager.desktop` += hyprland; workstation: system drops opencode, home adds opencode/clipboard/hyprland
- [x] Validated: parse OK; registry nixos 15 keys, homeManager 19 keys; toplevel + activation drvPaths evaluate; all 9 moved packages present in home.packages and absent from systemPackages
- [x] Post-switch login smoke test REQUIRED (user): waypaper restore, rclone mount, then Phase 3 brightness/print-screen/hypridle keybinds

## Validation status (Phase 2.5)

- [x] `nix eval .#modules` — registry: nixos = main, cmdLine, mime, battery, nvim, opencode, hyprland, display, awww, waypaper, rclone, fastfetch, hardwareConfig, network, hardware, audio, security; homeManager = main, cmdLine, nvim, git, tmux, yazi, ghostty, tui, zotero, showoff, awww, waypaper, rclone, fastfetch
- [x] `nix eval .#nixosConfigurations.workstation.config.home-manager.users.elichall.home.activationPackage.drvPath` — full home-manager config evaluates to a real drv (`/nix/store/6lhivpab6gfv5vi0ajwqmriy4vl06jqn-home-manager-generation.drv`)
- [x] Spot-check evals — session vars (FILEMANAGER/TERM_FILE_CHOOSER now via yazi), all 6 desktop entries, TUI binaries + LSPs + wrappers + icons in home.packages, Zotero dataFile, ghostty theme baseline all resolve
- [x] Post-package-placement evals — user profile: portal-gtk + all 10 showoff deps + libqalculate/chafa/fastfetch + ripdrag + tree-sitter + ripgrep/tree present; system: only unzip/wl-clipboard/xdg-utils/brightnessctl/grimblast/hypridle (+ feature modules); all 9 moved-out packages absent from systemPackages
- [x] Full `toplevel.drvPath` — VALIDATES after `nixos.hardwareConfig` + system/ split wiring (`34p57aphpnv6v1zzw76fc6kjga0g7lwi-nixos-system-t480-nixos-...drv`, changed by docker removal); activation drvPath `/nix/store/0xgpzn2jfzwjjlcxvadjw4zwj251znfy-home-manager-generation.drv`
- [x] Configuration-lean spot checks — pipewire/bluetooth/firewall/tlp/rtkit/fstrim/fwupd/earlyoom/kptr/hostName/sandbox all resolve; `dockerInPkgs = false`; tailscale port 41641 ref resolves
- [ ] Skeleton hosts (`wsl`/`ubuntu`/`macos`) — BLOCKED: use `specialArgs`; HM 26.05 requires `extraSpecialArgs` (see stretch below)

## Neovim nixification analysis (COMPLETE, doc-only: `_assets/plans/nvim-nixification-evaluation.md`)

- [x] Deep evaluation of Nixvim (2.9k⭐), NVF (1.6k⭐), NixCat's (699⭐, maintenance-mode), bare HM `programs.neovim` vs current Lua dotfiles — verdict: keep current architecture; Lua-native config is correct for this complexity level; recommend adding flake-input plugin pins for reproducibility without rewrite

## Noctalia Shell migration (COMPLETE, 2026-08-19)

Stable stack replaces hand-rolled display components on the workstation host:

- [x] Terminal abstraction: `modules/_lib/terminal.nix` (term/exec/execClass/packages) + `options.custom.terminal` NixOS option in `configuration.nix`; `terminalName` bridged via `home-manager.extraSpecialArgs`
- [x] `modules/display/desktop/foot.nix`: foot terminal (HM `programs.foot.enable`, Catppuccin Mocha palette, JetBrainsMono:size=7.5)
- [x] `modules/display/desktop/noctalia.nix`: Noctalia Shell (`programs.noctalia.enable`, dark mode, Catppuccin builtin, top bar with battery/volume/clock, dock disabled, wallpaper dir, clipboard, notification daemon)
- [x] `modules/hosts/workstation.nix`: `custom.terminal = "foot"`, noctalia.homeModules.default shared
- [x] `modules/hosts/laptop.nix`: `custom.terminal = "ghostty"`, experimental stack (`desktopExp` group)
- [x] `modules/hosts/server.nix`: empty stub
- [x] `modules/groups/desktop.nix` (stable): hyprland, foot, tui, noctalia, otterLauncher, showoff
- [x] `modules/groups/desktopExp.nix` (experimental): hyprland, ghostty, tui, otterLauncher, showoff, awww, waypaper, waybar, theme
- [x] Otter `--class` → `--app-id` migration: `_lib/terminal.nix` execClass branches (foot → `--app-id`, ghostty → `--class`); otter.nix uses `terminal.execClass`
- [x] Showoff ported to terminal abstraction: `terminalName`, `classFlag`, `terminal.term` injected at build; pgrep/pkill use `showoff\.` pattern; tmux config updated for foot
- [x] UPower battery: `services.upower.enable = true` in `system/battery.nix` (Noctalia battery widget reads D-Bus)
- [x] Noctalia autostart: `${noctaliaCmd} &` in hyprland autostart block via `inputs.noctalia.packages.x86_64-linux.default`
- [x] Noctalia config corrections: valid TOML settings only (validated with `noctalia config validate` zero warnings)
- [x] Foot config corrections for 1.27.0: `[colors]` → `[colors-dark]`, `[cursor].color` → `[colors-dark].cursor`, `[main].hide-when-typing` → `[mouse].hide-when-typing`, `single-instance` removed, font `JetBrains Mono` → `JetBrainsMono Nerd Font`
- [x] Tmux SUPER+T fix: `terminal.exec tmuxCmd` wraps bare tmux in foot window for keybind
- [x] Otter overlay fix: `chafa -s x8` → `chafa -s x4`, window rule `550x250` → `550x350`
- [x] Noctalia idle conflict resolved: `[idle.behavior.lock].enabled = false`, `[idle.behavior.screen-off].enabled = false`
- [x] Module-contracts.md updated: C1 (ghostty ownership experimental-only), C5 (foot --app-id migration), C10 (wallpaper experimental-only), C18 (showoff terminal abstraction), C22 (terminal abstraction), C23 (Noctalia config), C24 (UPower battery), C16 (groups updated), host wiring (3 hosts), registry map (foot, noctalia, desktopExp)
- [x] Decisions.md updated: #44-50 (Noctalia, foot, 3-host split, terminal abstraction, UPower, --class migration, showoff port)

## Research workflow (plan: `_assets/plans/research_workflow_stack.md`)

- [x] `modules/research/default.nix` → `homeManager.research`: pandoc HM module (citeproc + xelatex defaults) + texlive (slim: latexmk/biber/bibtex + pandoc template deps); user-scale only
- [x] `modules/research/obsidian.nix` → `homeManager.obsidian`: adaptive nvim feature — generates lean/research/init.lua (obsidian.nvim + blink-cmp-bibtex + blink.cmp source merge) + lean/research/lsp.lua (obsidian_ls); activation-merged into nvim config
- [x] `modules/_assets/dotfiles/nvim/init.lua`: pcall pattern for optional feature loading (lean.research)
- [x] `modules/_assets/dotfiles/nvim/lua/lean/plugins/lsp.lua`: feature-contributed LSP servers via pcall (lean.research.lsp)
- [x] `modules/_assets/dotfiles/nvim/lua/lean/plugins/completion.lua`: feature-contributed sources via lazy.nvim spec deep-merge
- [x] `modules/groups/research.nix` → `homeManager.researchGroup`: preset (research, obsidian, zotero)
- [x] `modules/hosts/workstation.nix`: wire researchGroup into user imports
- [x] `module-contracts.md`: registry rows (research, obsidian, researchGroup) + C20 vault path + C21 adaptive framework
- [x] `decisions.md`: decisions #34 (adaptive nvim) + #35 (research group)
- [x] `modules/display/otter-launcher/config.toml`: uncomment `obs` module → vault open in nvim
- [x] Better BibTeX plugin in Zotero → auto-export to vault `.bib` files

### Pending (vault restructuring)

- [x] `modules/research/obsidian.nix`: expand workspace from `research/` to vault root (`~/Documents/me/vault`)
- [x] `modules/research/obsidian.nix`: change `discover_bib_files()` to hierarchical walk via `global_files` function
- [x] `modules/research/default.nix`: add `.latexmkrc` for build artifact redirection to `.build/`
- [x] Update `modules/_assets/documentation/decisions.md`: #40 (vault root workspace + templates), #41 (BibTeX hierarchical walk — implemented via function)
- [x] Update `modules/_assets/documentation/module-contracts.md`: C20 (hierarchical bib via function, templates)
- [x] Update `modules/_assets/plans/research_workflow_stack.md`: hierarchical bib implemented

## Stretch goals

- [S] Quickshell-wip specialisation (`desktop-development.nix` port)
- [x] [S] Way-edges users guide (`modules/_assets/plans/wayedges-users-guide.md`): framework-validation doc for the hub plan (`modules/_assets/plans/wayedges.md`), version-pinned to way-edges 0.12.1 (commit ffcda9ee). Verified from binary schema + upstream source: widget model (btn/slider/wrap-box/workspace), boxed items (ring/text/tray ONLY — no terminal/ANSI item), templates (`{float:2,100}`, `{preset}`, `\{`), event-map keys (mouse-left..back / kc-0x…), pin model (togglepin matches namespace literally; colon = convention, no tab exclusivity), reload + inotify auto-reload. Hub verdicts: specs/toggles/volume/listings ✓, tabs ⚠ (scripted pin toggles), cava/ANSI-color ✗ (single-color cosmic-text; monochrome block art only). doc-only change; no build. Actual hub implementation = future task from this guide
- [S] opencode pdf-parse plugin (`~/.config/opencode/plugins/`) for automated PDF ingestion — deferred from `opencode-implementation.md`; `ocread` covers current need
- [S] Cross-platform hosts: fix `specialArgs` → `extraSpecialArgs`, import shared modules, feature-flag adaptation (isWSL/isDesktop/isServer)
- [S] otter overlay hardcode `/etc/nixos/assets/nixos-image.png` parameterization → use `modules/_assets/nixos-image.png` — DONE in phase-1 legacy diff (overlay_cmd → repo asset); final form at port
- [S] `cmdLine.nix` hm-session-vars `bashrcExtra` cleanup — DEFERRED: user reported issues with `hm-session-vars.sh`; do NOT touch without explicit go-ahead
- [S] `systemPackages` dedup across remaining modules — SUPERSEDED by AGENTS.md Rule 4 (duplicate package declarations are explicitly allowed/encouraged for dependency isolation); dedup is now optional cleanup only
- [S] `nixos.main` now base-only: bootloader/zram, time/locale, users, nix-ld, fonts, base systemPackages, flatpak, tmpfiles, nix.settings/gc (intentionally kept per user choice). docker REMOVED (unused); docker service split no longer needed
- [S] `state-implementation.md` (services.state.items bluetooth `Powered` persistence) — REACH, left as-is per user decision
